{
  "name": "zai-coding-plan-pi-package",
  "version": "0.1.4",
  "description": "Explicit Z.AI GLM Coding Plan provider for pi using the coding endpoint so usage counts against the plan quota, with a live quota indicator in the TUI.",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "files": [
    "index.ts",
    "usage-indicator.ts",
    "README.md"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "scripts": {
    "build": "echo 'No build step required'",
    "check": "node --test ./tests/*.test.ts",
    "test": "node --test ./tests/*.test.ts"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
