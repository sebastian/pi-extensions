{
  "name": "zai-coding-plan-pi-package",
  "version": "0.2.1",
  "description": "Enhances pi's built-in ZAI Coding Plan provider with quota status, concise prompt nudges, and conservative GLM-5.x context windows.",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "files": [
    "index.ts",
    "usage-indicator.ts",
    "README.md"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "scripts": {
    "build": "echo 'No build step required'",
    "check": "node --test ./tests/*.test.ts",
    "test": "node --test ./tests/*.test.ts"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
